import { styles } from "./styles";

export const MuiButton = {
  styleOverrides: {
    root: {
      textTransform: "none",
      borderRadius: styles.btnBorderRadius,
      fontFamily: "Poppins",
    },
  },
};
